<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CustomPicker"
             x:Class="CustomPicker.MainPage">
  <StackLayout>
    <Label Text="Welcome to Xamarin Forms!"
             VerticalOptions="Center"
             HorizontalOptions="Center" />
    <local:ComboPicker Title="Selecione um item..." ItemsSource="{Binding lstObjetos}" SelectedItem="{Binding Selecionado, Mode=TwoWay}" TextValue="Descricao" ></local:ComboPicker>
    <Button Text="Verificar" Clicked="btnVerificar_Clicked"/>
  </StackLayout>
</ContentPage>